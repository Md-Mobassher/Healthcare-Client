import { Button, Container } from "@mui/material";

const HomePage = () => {
  return (
    <Container className="min-h-screen">
      <h1>Homepage</h1>
    </Container>
  );
};

export default HomePage;
